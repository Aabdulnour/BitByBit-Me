# Future Work

## ML extensions
- Adaptive question selection that personalizes practice order in real time.
- Difficulty estimation so each question has a calibrated level for better routing.
- Advanced mastery modeling that blends diagnostics, streaks, and hint usage for richer insights.

## Ready Player Me integration
- Embedded editor launched directly from the profile page.
- Avatar synchronization across devices and sessions so every view stays in sync.

## Teacher features
- Exportable reports for admin stakeholders (CSV/PDF).
- Custom quiz authoring inside the UI so teachers can seed new diagnostics or mini quizzes.
